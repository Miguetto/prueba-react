import { BrowserRouter } from 'react-router-dom';
import { AppRouter } from './router/AppRouter';

const PelisSeries = () => {

  return (
    <BrowserRouter>
      <AppRouter />
    </BrowserRouter>
  )
};

export default PelisSeries;